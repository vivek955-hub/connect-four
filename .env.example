# MongoDB connection string - replace with your local/remote MongoDB URI
MONGODB_URI=mongodb://localhost:27017/connect_four

# Server port
PORT=4000

# CORS origin for frontend (adjust if frontend runs on different host/port)
CLIENT_ORIGIN=http://localhost:5173
